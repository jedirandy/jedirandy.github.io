language: node_js
node_js:
- node
script:
- npm run lint
- npm run build
deploy:
  provider: pages
  script: deploy
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: dist
  target_branch: master
  on:
    branch: src
